[package]
name = "agent"
version = "2.0.0"
edition = "2021"

[dependencies]
# Ya no necesitamos AES - conexión directa sin shellcode

[[bin]]
name = "agent"
path = "src/main.rs"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
# strip = true  # Descomentar para producción
